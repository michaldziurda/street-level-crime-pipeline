[project]
name = "street-level-crimes-pipeline"
version = "0.1.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["utils*", "etl*"]